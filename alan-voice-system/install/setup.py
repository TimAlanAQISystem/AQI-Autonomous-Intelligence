from setuptools import setup, find_packages

setup(
    name="alan-voice-system",
    version="1.0.0",
    description="Alan v1 — Human-First, Neg-Proof Voice System",
    packages=find_packages(),
    python_requires=">=3.10",
    install_requires=[],
)
